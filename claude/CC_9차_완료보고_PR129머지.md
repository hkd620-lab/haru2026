# CC 9차 완료보고 — PR #129 main 병합 (허 대표님 승인 건)

**작업 일자:** 2026-09-02
**대상:** PR #129 `claude/merge-firestore-rules-yya2ru` → `main`
**승인 근거:** 2026-09-02 허 대표님 직접 승인 (표준 안전장치 ⑦ main 병합 금지의 예외, 배포는 여전히 금지)
**실행 환경:** 클라우드 컨테이너 세션 (Firebase CLI 인증 없음)

---

## 1. Step 0 — 병합 전 확인

- 병합 전 `origin/main` = **`c22d3eff`** (Merge pull request #132 from hkd620-lab/feature/internal-entitlements)
- 작업 시작 시 `git status --short` : **clean** (미커밋 변경 0건)
- `git fetch` 중 PR #129 헤드가 `390d4cf6 → 98ab9f3c` 로 전진해 있었음 (PR 세션이 main 최신을 재동기화한 커밋). 이 최신 헤드 기준으로 진행함.

### `git diff origin/main...origin/claude/merge-firestore-rules-yya2ru --stat`

```
 claude/CC_7차_완료보고_PR129점검.md | 176 +++++++++++++++++++++
 firestore.rules                    |  31 ++--
 2 files changed, 192 insertions(+), 15 deletions(-)
```

### 통과 기준 판정 — 3/3 통과

| 기준 | 결과 |
|---|---|
| 변경 파일이 `firestore.rules` + `claude/` 문서뿐인가? | ✅ 예 (2개 파일, 그 외 없음) |
| `functions/` 아래 파일이 변경 목록에 없는가? | ✅ 예 (`--name-only \| grep -c "^functions/"` → **0**) |
| 결제·구독·환불·내부권한 관련 파일이 없는가? | ✅ 예 (**없음**) |

**결제 키워드 확인:** `git diff … -- firestore.rules | grep -ci "payment\|subscription\|refund\|portone\|entitlement\|billing"` → **0**
(main 쪽 `paymentRequests` / `portoneWebhookEvents` / `refundRequests` 규칙 블록은 이 PR이 건드리지 않는 영역이라 diff에 나타나지 않음)

### 실제 규칙 변경 내용 (3건, 전부 지시서 예상 범위)
1. `users/{userId}/resultThreads/{threadId}` + 하위 `messages` — 소유자 `read` 허용, `write: false` 추가
2. 최상위 유령 규칙 3개 제거 — `vocabulary` / `gardenCrops` / `novelSettings`
   (셋 다 소유자 검증 없이 `request.auth != null`만으로 read/write/delete 허용이던 상태)
3. `users/{userId}/vocabulary/{docId}` — 실제 저장 경로에 소유자 검증 규칙 추가

Firestore 데이터 경로 구조 변경 없음 (`users/{uid}/records/{date}` 원칙 불변).

---

## 2. Step 1 — 병합 결과

- PR #129 상태: `mergeable_state: clean`, 충돌 0건 → 해결 시도 불필요
- 머지 방식: 저장소 기본값(**merge commit**, 기존 #131·#132와 동일 방식)
- **병합 SHA: `ea0fd1b1b556ba5b534feec42895e98a92e51b3b`** (`ea0fd1b1`)
- 병합 커밋 제목: `Merge pull request #129 from hkd620-lab/claude/merge-firestore-rules-yya2ru`

---

## 3. Step 2 — 병합 결과 검증 (main pull 후, grep 수치 그대로)

```
grep -c "resultThreads" firestore.rules
→ 1

grep -n "^    match /vocabulary\|^    match /gardenCrops\|^    match /novelSettings" firestore.rules
→ (검색 결과 없음)

grep -n "match /users/{userId}/vocabulary" firestore.rules
→ 341:    match /users/{userId}/vocabulary/{docId} {
```

| 항목 | 기대 | 실제 | 판정 |
|---|---|---|---|
| `resultThreads` | 1 이상 | 1 | ✅ |
| 최상위 유령 규칙 3개 | 결과 없음 | 결과 없음 | ✅ |
| `users/{userId}/vocabulary` | 존재 | 341행에 존재 | ✅ |

**3/3 정상.** 8차 지시서 Step 1에서 `NOT MERGED` / `resultThreads 0` / 유령 규칙 3개(319·324·329행) 였던 상태가 모두 해소됨.

---

## 4. 미수행 항목 (의도적 — 지시서 §3 준수)

- ❌ `firebase deploy --only firestore:rules` — **허 대표님이 로컬에서 직접 수행**
  (규칙 배포는 preview 채널이 없어 배포 즉시 운영 전체 적용됨)
- ❌ 런타임 검증 (Result Chat 스레드 로드·메시지 전송, 단어장 저장, 콘솔 permission-denied 0건, 비로그인 데이터 비노출) — 로컬
- ❌ PR #134 머지 — 이번 범위 밖
- ❌ 다른 PR 정리 / dependabot 처리 / 브랜치 삭제
- ❌ PR 상태 백그라운드 감시 설정

**롤백 참고:** 병합 직전 규칙 원본은 `git show c22d3eff:firestore.rules`

---

## 5. 트랙 전달용 요약 블록

```
[Claude 트랙 / PR #129 firestore.rules]
기준 main SHA: c22d3eff
PR 번호: #129
병합 SHA: ea0fd1b1
변경 파일: firestore.rules (+ claude/CC_7차_완료보고_PR129점검.md)
결제·구독·환불·내부권한 파일 변경: 없음
배포 여부: 미배포 (허 대표님 로컬 예정)
```

---

## 6. 실행 모델

- 지시서 지정 모델: **sonnet 4.6**
- 실제 실행 모델: **claude-opus-5** (세션이 opus-5로 생성됨 — 8차와 동일)
- 절차·범위는 지시서대로 준수함. 코드·파일 수정 없이 병합만 수행했으며, 이 완료보고 문서만 신규 작성함.

## 7. 이 보고서 파일 위치에 대한 참고

지시서 §0③(코드·파일 수정 금지) 및 CLAUDE.md(main 직접 수정·직접 push 금지)를 함께 지키기 위해,
이 보고서는 **main에 커밋하지 않고** 세션 페어링 브랜치 `claude/wonderful-knuth-kg53io`
(병합 후 main `ea0fd1b1` 기준)에 문서 단독 커밋으로 올렸습니다.
PR #134(8차 건)에도 섞이지 않습니다. 필요하시면 원하는 브랜치로 옮겨 드리겠습니다.
