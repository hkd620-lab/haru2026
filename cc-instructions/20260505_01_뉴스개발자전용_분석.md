# CC 지시서 — 개발자 전용 기능 현황 분석 (1차)

## 작업 개요
허 교장님 작업 — 개발자 전용 기능 정리 및 뉴스 수동화 준비

이번 단계는 **분석만** 진행해줘. 코드 수정은 절대 금지야.

## 작업 전 안전 저장 (이미 완료했지만 한번 더 확인)

```bash
cd ~/HARU2026 && git status
```

## 분석 요청 사항

다음 4가지를 표 형식으로 정리해서 보고해줘:

### 1. 개발자 UID 체크 위치 전수 조사

개발자 UID는 다음과 같아:
```
naver_lGu8c7z0B13JzA5ZCn_sTu4fD7VcN3dydtnt0t5PZ-8
```

이 UID가 코드에서 체크되는 모든 위치를 찾아줘:
- `~/HARU2026/functions/src/` 폴더 전체
- `~/HARU2026/frontend/src/` 폴더 전체

각 위치마다 다음을 알려줘:
- 파일 경로
- 줄 번호
- 어떤 함수/페이지에서 체크하는지
- 체크 후 어떻게 처리하는지 (차단? 허용? 다른 동작?)

### 2. 다음 기능들의 현재 접근 제한 상태

| 기능 | 위치 (파일/줄) | 현재 접근 제한 방식 | 일반 사용자 접근 가능? |
|---|---|---|---|
| 뉴스 (NewsPage) | | | |
| fetchTopNews 함수 | | | |
| refreshNews 함수 | | | |
| 하루AI지식창고 | | | |
| 책스튜디오 (BookCreate) | | | |
| generateBook 함수 | | | |
| generateTitlesForAll | | | |
| 영어성경 🔍 오류수정 버튼 | | | |

### 3. 라우터 및 메뉴 노출 상태

- `~/HARU2026/frontend/src/app/App.tsx`에서 위 기능들의 라우트 등록 상태
- 홈화면 (HomePage)에서 일반 사용자에게 메뉴로 보이는지
- 사이드바/네비게이션에서 보이는지

### 4. fetchTopNews 자동 스케줄 설정 상세

- `~/HARU2026/functions/src/index.ts:2138` 부근의 fetchTopNews 함수 분석
- 스케줄 트리거 설정 확인 (몇 분/시간마다? Cloud Scheduler? onSchedule?)
- 스케줄 비활성화 방법 안내 (코드 수정만으로 가능한지, gcloud 명령어가 필요한지)
- refreshNews 함수가 누가 호출 가능한 구조인지 (인증 필요? 누구나 호출 가능?)

## 보고 형식

위 4가지 항목을 표 형식으로 깔끔하게 정리해서 보고해줘.
허 교장님이 한눈에 볼 수 있게 부탁해.

## ⚠️ 주의사항

- **코드 수정 절대 금지** — 분석만 해줘
- **파일 생성/삭제 금지**
- **배포 금지**
- **기존 함수/import 건드리지 말 것**
- 분석 결과만 텍스트로 보고

분석 완료 후 허 교장님이 검토하고 다음 단계(2차 수정 지시서)로 넘어갈 거야.
